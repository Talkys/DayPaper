#!/bin/bash

#PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

./sel -3 "Caminho da pasta dos wallpapers" "Caminho da pasta do now.png"

#Detalhe, tem que tirar as aspas e deixar só o caminho, mas acho que isso não vai bugar em todos os sistemas, depende de como o bash se comporta aí caso vc tenha o configurado
